<%= render 'shared/errors', object: @product %>

<%= form_for [:admin, @product] do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :price %>
  <%= f.text_field :price %>

  <%= f.submit "Save" %>
<% end %>
